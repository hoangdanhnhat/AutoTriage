---
- name: Verify Connectivity to Linux Nodes
  hosts: linux_nodes
  gather_facts: false
  tasks:
    - name: Ping Linux hosts
      ansible.builtin.ping:

- name: Verify Connectivity to Windows Nodes
  hosts: windows_nodes
  gather_facts: false
  tasks:
    - name: Ping Windows hosts (via SSH/CMD)
      ansible.windows.win_ping:
